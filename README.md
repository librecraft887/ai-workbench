# AI教研助手 V1.3.1｜客户方案文风与排版优化

本版按实际培训项目的业务顺序运行：互动提问明确需求 → 调研与本次培训有关的客户背景 → 综合分析并设计配课。调研结果参与课程选择和安排，页面、复制文本、Word 共用“项目背景”“客户需求分析”“方案设计逻辑”三段正文。

本次进一步统一面向客户的写作方式：直接连接客户的工作场景与培养目标，正文不出现检索口径、来源编号或库内配课说明；设计逻辑以模块依据和学习路径为主。正文适当分段，首行缩进两个字符。培训概览补全教学方式和预期成果，无实际依据时不显示“查看依据”。

## 部署到 GitHub / EdgeOne

上传或替换以下文件，并提交到 `main`：

- `assistant.html`
- `index.html`（保留资源维护助手的预留扩展位）
- `edge-functions/api/chat.js`
- `edge-functions/lib/research/service.js`
- `edge-functions/lib/research/providers/bocha.js`
- `edge-functions/lib/proposal.js`（新增，三段分析提示词与来源校验）
- `assets/jszip.min.js`
- `assets/research-display.js`
- `assets/research-session.js`
- `assets/training-workflow.js`（新增，需求→调研→设计流程）
- `assets/proposal-display.js`（新增，页面与导出的统一方案正文）
- `assets/client-copy.js`（新增，客户文风清理、分段、完整概览与真实依据显示）
- `assets/research-export-integration.js`

前后端文件必须在同一次提交中上传，防止旧页面与新接口混用。`tests/`、`docs/` 和操作说明仅用于开发验收与阅读，不影响线上运行。详细上传步骤见 `上传到GitHub_操作步骤.md`。

## 环境变量

- `DEEPSEEK_API_KEY`
- `CLOUDBASE_ENV_ID`
- `CLOUDBASE_API_KEY`
- `BOCHA_API_KEY`（备用联网检索）

## 工作流程

1. 在新项目中填写客户名称，并在对话框录入培训需求。
2. 客户名称、培训对象、培训主题、天数、培养目标/希望解决的问题缺失时，用互动提问补充；已明确的信息保留，不重复追问。用户可明确选择“暂不提供客户”或“请建议培养目标”。
3. 业务问题、学员基础、师资偏好和教学组织要求一并吸收；未说明时不猜测。课次缺失按每天上午、下午各一讲安排，并在方案概览注明。
4. 需求完整后才调研。主检索围绕培训主题检索客户战略、业务和人才培养；主链路失败时，博查的四组问题同样带入培训主题和对象。
5. 调研完成后，才加载师资课程数据并生成三段分析及配课。资料不足时只依据已确认需求设计，页面说明资料情况。
6. “重新调研并更新方案”重新执行调研与设计，更新正文及课表。需求、客户或培养目标变化时，旧调研缓存失效。

## 客户调研输出

每次调研围绕以下维度：

- 客户画像：机构性质、业务范围；
- 战略与业务：中长期战略、年度重点、近期业务信号；
- 人才与能力：干部培养、培训体系、数字化及岗位能力线索；
- 培训设计启示：必须对应至少一个来源编号，不代表客户已经提出未被公开资料证明的内部需求；
- 公开资料来源：标题、证据、URL、发布日期（如有）。

主检索的 URL 必须有联网响应中的引用支撑；没有可验证引用时，主检索不会保留模型写出的 URL。备用博查结果直接保留供应商返回的 URL。所有链路均不向前端显示供应商错误、Key、HTTP 响应或堆栈。

## 页面、Word 与项目隔离

- 正文先展示三段连贯分析：项目背景（为什么开展）、客户需求分析（提升什么能力）、方案设计逻辑（为何采用这些模块、顺序和教学方式），自然过渡到配课方案。
- 每行课程适当补充学习重点和能力目标；模块组合的整体理由在设计逻辑正文表达，不逐门重复展开。
- 原始检索摘要及关联来源编号仅供内部核验；页面末尾的“调研资料（内部参考）”可展开阅读。Word 与复制文本不导出资料附录、来源编号或内部操作说明。
- 正文首行缩进两个字符，较长分析按逻辑分段；页面、复制文本和 Word 使用相同分段内容。
- 培训概览共用完整需求和方案补充的教学方式、预期学习成果，不展示空字段或“—”。
- 课程依据只采用实际授课关系备注和已关联的调研资料链接；“正式测试关系”等占位备注不显示，没有实际依据时不显示“查看依据”。
- 同一项目的培训需求发生实质变化时，系统会按新需求重新调研，不复用旧的课程设计启示。
- 异步请求会绑定发起项目；用户切换项目后，旧请求不会污染新项目。
- Word 和“复制文本”共用同一方案章节，顺序为：项目背景、客户需求分析、方案设计逻辑、培训安排概览、配课方案、推荐师资简介、备选师资（待确认）。
- 来源关联仅存在内部字段；备用摘要未经原文核验时，不写未经确认的具体战略、数字或官方定性，围绕已确认目标组织自然的方案语言。
- 旧项目没有三段正文时，应先补充培养目标并更新方案，再复制或导出 Word。
- 正式课表仍只使用库内、状态有效且教师课程关系已确认的数据；外部候选只作补充，不能进入正式课表。

## 本地验证

在项目目录执行：

```powershell
node --test tests/*.test.mjs
node --check edge-functions/api/chat.js
node --check edge-functions/lib/research/service.js
```

验收覆盖：需求与调研流程、客户正文隐藏引用和内部说明、两字符缩进与分段、概览补全、占位依据隐藏及真实链接关联、实际 Word 导出、项目隔离、博查响应和师资简介边界。
